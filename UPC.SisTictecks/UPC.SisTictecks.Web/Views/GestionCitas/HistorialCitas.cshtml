@using UPC.SisTictecks.Helpers;
@model IEnumerable<UPC.SisTictecks.EL.CitaEN>

@{
    ViewBag.Title = "HistorialCitas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Historial de Citas</h2>
<hr />

<table class="table">
    <tr>       
        <th>
            @Html.DisplayNameFor(model => model.NroCita)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoraInicio)
        </th>        
        <th>
            @Html.DisplayNameFor(model => model.HoraFin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Observacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NroCita)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoraInicio)
        </td>        
        <td>
            @Html.DisplayFor(modelItem => item.HoraFin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Observacion)
        </td>
        <td>
            @if (item.Estado == (int)eEstadosCita.Pendiente)
            {
                <span>Pendiente</span>
            }
            else if (item.Estado == (int)eEstadosCita.realizado)
            {
                <span>Realizado</span>
            }
            else if (item.Estado == (int)eEstadosCita.Cancelado)
            {
                <span>Cancelado</span>
            }
        </td>        
    </tr>
}

</table>
